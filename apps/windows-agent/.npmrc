# Allow electron and ffmpeg-installer postinstall scripts to run during build
# (electron-builder runs `pnpm install` internally during packaging)
dangerously-allow-all-builds=true
strict-peer-dependencies=false
